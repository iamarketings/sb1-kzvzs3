<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Portal</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-content">
            <div class="nav-brand">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2a5 5 0 0 1 5 5v6a5 5 0 0 1-10 0V7a5 5 0 0 1 5-5Z"/><path d="M19 10v2a7 7 0 0 1-14 0v-2"/><line x1="12" y1="19" x2="12" y2="22"/></svg>
                AI Portal
            </div>
            <button class="menu-toggle" aria-label="Toggle menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <div class="nav-links">
                <a href="/" class="<%= path === '/' ? 'active' : '' %>">Accueil</a>
                <a href="/services" class="<%= path === '/services' ? 'active' : '' %>">Services</a>
                <a href="/contact" class="<%= path === '/contact' ? 'active' : '' %>">Contact</a>
                <% if (locals.user) { %>
                    <a href="/dashboard" class="<%= path === '/dashboard' ? 'active' : '' %>">Dashboard</a>
                    <a href="/logout" class="btn-logout">Déconnexion</a>
                <% } else { %>
                    <a href="/login" class="btn-login">Connexion</a>
                <% } %>
            </div>
        </div>
    </nav>
    <% if (locals.error) { %>
        <div class="alert error"><%= error %></div>
    <% } %>
    <% if (locals.success) { %>
        <div class="alert success"><%= success %></div>
    <% } %>
    <script src="/js/main.js"></script>